terraform {
  backend "s3" {
    bucket         = "security-scanner-terraform-state"
    key            = "env/dev/terraform.tfstate"
    region         = "us-west-2"
    dynamodb_table = "security-scanner-terraform-locks"
    encrypt        = true
  }
}
